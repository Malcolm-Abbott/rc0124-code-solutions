/* exported getFirstChar */
/* Pseudo Code
Define a function named getFirstChar that accepts one parameter: string
Write a return statement which returns a value of the string at 0 index.
*/
function getFirstChar(string: string): string {
  return string[0];
}
