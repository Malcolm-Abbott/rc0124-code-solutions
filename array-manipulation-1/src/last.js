'use strict';
/* exported last */
/*
Define a function named last that accepts one paramter: an array | list of any data type
Using the appropriate method or indexing, return the last element
*/
function last(array) {
  return array.at(-1);
}
